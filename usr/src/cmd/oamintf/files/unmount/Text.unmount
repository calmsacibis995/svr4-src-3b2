#ident	"@(#)filemgmt:unmount/Text.unmount	1.5"
Title="$ARG1"
close=`/bin/rm /tmp/umnt 2>/dev/null`CLOSE

`readfile $INTFBASE/display.msg | message -p`

help=OPEN TEXT $INTFBASE/Text.itemhelp unmounttask

begrow=distinct
begcol=distinct
rows=5
columns=75

text="`readfile $ARG2`"
